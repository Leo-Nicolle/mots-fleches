
module.exports = {
  componentsRoot: 'client/src/', // the folder where CLI will start searching for components.
  components: '**/*.vue', // the glob to define what files should be documented as components (relative to componentRoot)
  outDir: 'docs/client', // folder to save components docs in (relative to the current working directry)
}